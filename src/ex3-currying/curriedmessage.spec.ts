// test harness
import "mocha";
import { expect } from "chai";

// under test
import { sendRequest } from "./curriedmessage";

describe("Messages using currying", function () {
    // it("Should create curried messages", function () {
    //   expect(sendRequest("Hello")("Status")("This is a message")).to.equal("Hello, Status : This is a message");
    // });
});
